# Base requirements with fixed versions to avoid compatibility issues
Flask==2.0.1
Werkzeug==2.0.1
flask-cors==3.0.10
requests==2.28.2
python-dotenv==1.0.0
flask-limiter==3.1.0
limits==2.8.0

# For better caching management
cachetools==5.3.0
python-json-logger==2.0.7  # For structured JSON logging

# For production deployment
gunicorn==20.1.0